//! @file
#ifndef STRING_TO_UPPER_H
#define STRING_TO_UPPER_H
/**
 * @brief Converts a string to uppercase
 *
 * Converts each character in the input string to its uppercase equivalent.
 *
 * @param string The input string to convert to uppercase
 * @return char* Returns the modified string in uppercase
 */
char* string_to_upper(char* string);

#endif
