#include <datatypes/datatypes.h>
#include <stdbool.h>
#include <stdio.h>

int query_7(Catalogs CATALOGS, int command_number, bool format_flag, char* top_n_airports) {
  printf("I'm in query_7\n");

  UNUSED(CATALOGS);
  UNUSED(command_number);
  UNUSED(format_flag);
  UNUSED(top_n_airports);
  return 0;
}